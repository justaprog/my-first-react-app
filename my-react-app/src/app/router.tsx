import { MeasurementsDashboardPage } from "../features/measurements/pages/MeasurementsDashboardPage";

// This file is intentionally simple for now.
// Later, you can replace this with react-router-dom if you add multiple pages.
export function Router() {
  return <MeasurementsDashboardPage />;
}
